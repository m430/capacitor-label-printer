import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.m430.capacitor.labelprinter.example',
  appName: 'LabelPrinterExample',
  webDir: 'dist',
};

export default config;
